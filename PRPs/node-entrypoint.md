# PRP — Node Entrypoint, Config Loader, Service Wrapper, and Release Workflow

**Status:** pending approval
**Spec reference:** docs/specs/tetherdb.md §5.10, §5.13, §6 (steps 7 + 9)
**Implements:** `cmd/tetherdb/main.go`, `internal/config/`, `kardianos/service` integration, GitHub Actions release

---

## FEATURE

The `tetherdb` binary entrypoint: parses CLI flags and subcommands, loads and validates a TOML config file (with environment variable interpolation), wires `kardianos/service` for Windows Service / Linux systemd / macOS launchd, and produces release binaries (Linux amd64 + Windows amd64) via GitHub Actions on every push to `main`.

---

## OBJECTIVE

When a user runs `tetherdb` with no arguments, it prints help and exits 0. When run with `--config ./tetherdb.toml` (or the default config path), it loads and validates the config, calls `Probe()` on the configured connector to verify connectivity, then starts the node and blocks until a signal arrives. Service management subcommands (`install`, `uninstall`, `start`, `stop`, `run`) are routed through `kardianos/service`. On every push to `main`, GitHub Actions builds and uploads two release artifacts: `tetherdb-linux-amd64` and `tetherdb-windows-amd64.exe`.

---

## CONTEXT

- Starting state: `internal/connector/` and `internal/connector/sqlserver/` exist and are tested. No `cmd/` directory exists. No `go.sum` entries for `kardianos/service` or `BurntSushi/toml`.
- Ending state: `cmd/tetherdb/main.go`, `internal/config/config.go`, `.github/workflows/release.yml` exist and work.
- Related spec: `docs/specs/tetherdb.md`
- Must not touch: `.env`, `go.sum` (generated by `go mod tidy`), `LICENSE`, `internal/connector/`
- Dependent components (pipeline engine, transport, state layer) do not exist yet — the node's `Start()` function stubs them out with a blocking `select{}` after `Probe()` succeeds.

---

## CLI INTERFACE

```
tetherdb [--config PATH] [--version] [SUBCOMMAND]

Subcommands:
  run         Start the node in the foreground (default when invoked by the service manager)
  install     Install tetherdb as a system service
  uninstall   Remove the system service
  start       Start the installed service
  stop        Stop the running service
  version     Print version and exit

Flags:
  --config PATH   Path to TOML config file (default: ./tetherdb.toml)
  --version       Print version and exit

No arguments: print help and exit 0.
```

The version string is injected at build time via `-ldflags "-X main.version=..."`. When not injected (local dev builds), it falls back to `"dev"`.

---

## CONFIG FILE STRUCTURE

Matches spec §5.10 exactly. The config package parses this TOML and exposes typed Go structs. Environment variable interpolation: any value of the form `${VAR_NAME}` is replaced with the value of `os.Getenv("VAR_NAME")` before the struct is populated. Interpolation applies to string fields only.

```toml
[node]
name     = "erp-node"
data_dir = "/var/lib/tetherdb"

[listen]
address  = "0.0.0.0:443"
tls_cert = "/etc/tetherdb/cert.pem"
tls_key  = "/etc/tetherdb/key.pem"

[management]
address = "127.0.0.1:8080"

[connector]
driver   = "sqlserver"
host     = "sqlserver.internal"
port     = 1433
database = "erp"
auth     = "sqlserver"
username = "${TETHERDB_SQL_USER}"
password = "${TETHERDB_SQL_PASS}"

[connector.publish]
tables = ["orders", "customers", "products"]

[[connections]]
name      = "primary-sink"
address   = "sink-node.internal:443"
subscribe = ["orders", "customers", "products"]
```

**Validation rules (checked at startup, before any connection):**

A node must have at least one of `[connector]` or `[listen]` — a config with neither is an error (the node would do nothing).

| Field | Rule | Condition |
|---|---|---|
| `node.name` | non-empty, alphanumeric + hyphens only | always |
| `node.data_dir` | non-empty string (existence not checked — created on first use) | always |
| `listen.address` | valid `host:port` format | only if `[listen]` is present |
| `listen.tls_cert` / `tls_key` | non-empty; file existence checked at startup | only if `[listen]` is present |
| `management.address` | valid `host:port`; must not be `0.0.0.0:*` | always |
| `connector.driver` | must be `"sqlserver"` (only supported driver in v1) | only if `[connector]` is present |
| `connector.host` | non-empty | only if `[connector]` is present |
| `connector.port` | 1–65535 (default 1433) | only if `[connector]` is present |
| `connector.auth` | `"sqlserver"` or `"windows"` | only if `[connector]` is present |
| `connector.username` | required when `auth = "sqlserver"` | only if `[connector]` is present |
| `connector.publish.tables` | at least one entry | only if `[connector]` is present |
| `connections[].name` | non-empty, unique across connections | only if `[[connections]]` entries exist |
| `connections[].address` | valid `host:port` | only if `[[connections]]` entries exist |
| `connections[].subscribe` | at least one entry; each must appear in `connector.publish.tables` | only if `[[connections]]` entries exist |
| cross-section | `[[connections]]` requires `[connector]` — a node cannot forward changes it does not read | always |
| cross-section | at least one of `[connector]` or `[listen]` must be present | always |

This means three valid node shapes are possible:

- **Source node** — has `[connector]` + `[[connections]]`, no `[listen]` required (dials out only)
- **Sink node** — has `[listen]`, no `[connector]`, no `[[connections]]` (accepts inbound only)
- **Relay node** — has both `[connector]` + `[[connections]]` + `[listen]` (reads, forwards, and accepts)

Password is never logged. Management address must not bind to `0.0.0.0` — error if attempted.

---

## IMPLEMENTATION REQUIREMENTS

### Must Do

- Create `cmd/tetherdb/main.go` — entrypoint, flag parsing, subcommand dispatch
- Create `internal/config/config.go` — Config struct, `Load(path string) (*Config, error)`, env var interpolation, validation
- Register `kardianos/service` with the binary; route `install`/`uninstall`/`start`/`stop`/`run` through the service interface
- The service `Start()` method: loads config, calls `sqlserver.New()` + `Probe()` only when `[connector]` is present, logs startup line, then blocks on a `context.Context` that is cancelled on `Stop()`
- If `Probe()` fails at startup, log the error and return it — do not start the service
- If `[connector]` is absent (sink-only node), skip `Probe()` and proceed to blocking — the connector will be wired in a future PRP
- Print help text (listing all subcommands and flags) when no arguments are provided
- Inject version at build time via `-ldflags "-X main.version=v0.1.0"`; fall back to `"dev"`
- Create `.github/workflows/release.yml` — triggers on push to `main`; builds `linux/amd64` and `windows/amd64` with `CGO_ENABLED=0`; strips debug info with `-ldflags="-s -w -X main.version=${VERSION}"`; uploads both binaries as workflow artifacts; version is derived from `git describe --tags --always --dirty`
- Every exported symbol must have a doc comment per `docs/CODE_STYLE.md`
- `CGO_ENABLED=0` at all times — no CGO anywhere

### Must NOT Do

- Do not implement the pipeline engine, transport, WebSocket, state layer, or management API — those are future PRPs
- Do not hardcode any host, port, credential, or path
- Do not log passwords or interpolated credential values
- Do not bind the management API listener in this PRP — just validate the address from config
- Do not panic on config errors — return errors to the caller
- Do not use `os.Exit` directly outside of `main()` — the service wrapper handles process lifecycle

---

## FILE STRUCTURE

```
cmd/
  tetherdb/
    main.go               ← entrypoint, flag parsing, subcommand dispatch
internal/
  config/
    config.go             ← Config struct, Load(), interpolate(), Validate()
    config_test.go        ← unit tests
.github/
  workflows/
    release.yml           ← build + artifact upload on push to main
```

---

## ERROR HANDLING REQUIREMENTS

| Situation | Behaviour |
|---|---|
| Config file not found | `Load()` returns descriptive error naming the path; node exits with non-zero code |
| Config file invalid TOML | `Load()` returns parse error with line number from TOML library |
| Validation failure | `Validate()` returns error naming the failing field and rule |
| `${VAR}` references undefined env var | interpolation leaves the value empty; validation catches required-field violations |
| `Probe()` fails at startup | log error, service `Start()` returns error, process exits non-zero |
| Unknown subcommand | print help and exit non-zero |

All errors wrapped with `fmt.Errorf("config: <context>: %w", err)` or `fmt.Errorf("main: <context>: %w", err)`.

---

## SECURITY CONSIDERATIONS

- Passwords and credentials must never appear in log output — log only `connector.username` and redact the password
- `management.address` must never be `0.0.0.0:*` — validate and error if attempted
- TLS cert and key file paths are validated for existence at startup — do not silently ignore missing files
- Env var interpolation must not expand arbitrary shell syntax — only `${VAR_NAME}` patterns (alphanumeric + underscore names); reject malformed patterns

---

## TESTS TO WRITE

All tests go in `internal/config/config_test.go`. No real filesystem or service manager required — tests use `t.TempDir()` for config files.

- [ ] `TestLoad_ValidSourceNode` — a full source-node TOML (connector + connections, no listen) loads without error
- [ ] `TestLoad_ValidSinkNode` — a sink-node TOML (listen only, no connector, no connections) loads without error
- [ ] `TestLoad_FileNotFound` — returns error when path does not exist
- [ ] `TestLoad_InvalidTOML` — returns parse error for malformed TOML
- [ ] `TestLoad_EnvVarInterpolation` — `${VAR}` in username/password fields is replaced with the env var value
- [ ] `TestLoad_EnvVarMissing` — `${VAR}` referencing an unset env var produces an empty string (validation then catches required fields)
- [ ] `TestValidate_MissingNodeName` — returns error for empty `node.name`
- [ ] `TestValidate_InvalidManagementAddress` — returns error when `management.address` is `0.0.0.0:8080`
- [ ] `TestValidate_UnknownDriver` — returns error for unrecognised `connector.driver`
- [ ] `TestValidate_SubscribeNotPublished` — returns error when a connection subscribes to a table not in `connector.publish.tables`
- [ ] `TestValidate_DuplicateConnectionName` — returns error when two connections share a name
- [ ] `TestValidate_MissingTLSFiles` — returns error when cert or key path does not exist on disk
- [ ] `TestValidate_ConnectionsWithoutConnector` — returns error when `[[connections]]` is present but `[connector]` is absent
- [ ] `TestValidate_NeitherConnectorNorListen` — returns error when both `[connector]` and `[listen]` are absent

---

## ROLLBACK PLAN

- Branch to return to: `main`
- No migrations
- State to return to: delete `cmd/`, `internal/config/`, `.github/`

---

## ACCEPTANCE CRITERIA

- [ ] `CGO_ENABLED=0 go build ./...` passes
- [ ] `go test ./internal/config/...` passes — all 13 tests green
- [ ] `go vet ./...` passes
- [ ] Running `./tetherdb` with no arguments prints help and exits 0
- [ ] Running `./tetherdb --config missing.toml` exits non-zero with a clear error
- [ ] Running `./tetherdb version` prints the version string and exits 0
- [ ] GitHub Actions workflow file is valid YAML and uses `CGO_ENABLED=0`
- [ ] Workflow uploads two artifacts: `tetherdb-linux-amd64` and `tetherdb-windows-amd64.exe`
- [ ] No password appears in any log output in tests or startup logs

## VALIDATION

```bash
CGO_ENABLED=0 go build ./...
go test ./internal/config/...
go vet ./...
./tetherdb               # should print help
./tetherdb version       # should print version
./tetherdb --config /nonexistent.toml  # should exit non-zero with clear error
```
